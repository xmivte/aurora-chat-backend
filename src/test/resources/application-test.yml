spring:
  flyway:
    enabled: true
    schemas: db
    default-schema: db
    locations: classpath:db/migrations

mybatis:
  configuration:
    map-underscore-to-camel-case: true

firebase:
  enabled: false