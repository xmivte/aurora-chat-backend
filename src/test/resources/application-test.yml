spring:
  flyway:
    enabled: true
    schemas: db
    default-schema: db
    locations: classpath:db/migrations
  task:
    scheduling:
      enabled: false

mybatis:
  configuration:
    map-underscore-to-camel-case: true

firebase:
  enabled: false
  credentials: classpath:test-firebase.json

cloudinary:
  credentials: classpath:test-cloudinary.json