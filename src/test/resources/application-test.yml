spring:
  datasource:
    url: jdbc:postgresql://localhost/testdb?currentSchema=db
    driver-class-name: org.postgresql.Driver

  flyway:
    enabled: true
    schemas: db
    default-schema: db
    locations: classpath:db/migrations